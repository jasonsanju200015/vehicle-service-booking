# Frontend - Vehicle Service Booking System

1. Install dependencies: `npm install`.
2. Start: `npm start`.

Configure API base URL in `src/api/axios.js` if backend is not at `http://localhost:5000`.
